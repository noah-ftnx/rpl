#include <queue>
#include <vector>
using namespace std;

// IMPLEMENT: merge k sorted arrays
vector<int> mergeKSortedArrays(vector<vector<int>>&kArrays, int k);

#include "test/05.h"
int main() { run_tests(); return 0; }
